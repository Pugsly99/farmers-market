import React from "react";

function NewContactForm(){
  return(
    <h1> placeholder for NewContactForm </h1>
  );
}

export default NewContactForm;

// will need a Class component eventually to update state of a new Contact form